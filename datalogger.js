// Array: can hold varible different numbers, strings etc (information); Area in memory to...
var numberValues;
//numberValues = [1,5,9,2];

function addANewNumber()
{
  //get text input from the DOM and store in a variable
  //check if that variable is a number
  //IF it is, add to the back o fthe Array
  
}
